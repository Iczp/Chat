using {{namespace}}.EntityFrameworkCore;
using IczpNet.{{pascalCase project}}.Repositories;
using System;
using Volo.Abp.EntityFrameworkCore;

namespace {{namespace}}.{{pascalCase pluralName}};

/// <inheritdoc />
public class {{pascalCase name}}Repository(IDbContextProvider<{{pascalCase project}}DbContext> dbContextProvider) : {{pascalCase project}}RepositoryBase<{{pascalCase name}}, {{pascalCase primaryKey}}>(dbContextProvider), I{{pascalCase name}}Repository
{
    
}


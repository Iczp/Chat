using FluentValidation;
using {{namespace}}.Localization;
using {{namespace}}.{{pascalCase pluralName}};
using Microsoft.Extensions.Localization;

namespace {{namespace}}.{{pascalCase pluralName}};

public class {{pascalCase name}}CreateInputValidator : AbstractValidator<{{pascalCase name}}CreateInput>
{
    public {{pascalCase name}}CreateInputValidator(IStringLocalizer<{{pascalCase project}}Resource> _localizer)
    {

        //RuleFor(x => x.Name)
        //   .NotEmpty()
        //   .WithMessage(_localizer["{{pascalCase project}}"]);
    }
}

using FluentValidation;
using {{namespace}}.Localization;
using {{namespace}}.{{pascalCase pluralName}};
using Microsoft.Extensions.Localization;

namespace {{namespace}}.{{pascalCase pluralName}};

public class {{pascalCase name}}UpdateInputValidator : AbstractValidator<{{pascalCase name}}UpdateInput>
{
    public {{pascalCase name}}UpdateInputValidator(IStringLocalizer<{{pascalCase project}}Resource> _localizer)
    {

        //RuleFor(x => x.Name)
        //   .NotEmpty()
        //   .WithMessage(_localizer["{{pascalCase project}}"]);
    }
}
